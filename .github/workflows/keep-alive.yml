name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping All Projects
        run: |
          # 🔥 Flames Finder
          curl -s -X GET "https://teziphucpvvqghwhcvyq.supabase.co/rest/v1/flames_calculations?select=id" \
          -H "apikey: ${{ secrets.SUPABASE_KEY_1 }}" \
          -H "Authorization: Bearer ${{ secrets.SUPABASE_KEY_1 }}"

          # 🛒 Ecommerce
          curl -s -X GET "https://ifxofuzxczwrvhlxzbgz.supabase.co/rest/v1/products?select=id" \
          -H "apikey: ${{ secrets.SUPABASE_KEY_2 }}" \
          -H "Authorization: Bearer ${{ secrets.SUPABASE_KEY_2 }}"

          # ⚡ Third Project
          curl -s -X GET "https://rnkretlgnqqxipnnbgqf.supabase.co/rest/v1/hotels?select=id" \
          -H "apikey: ${{ secrets.SUPABASE_KEY_3 }}" \
          -H "Authorization: Bearer ${{ secrets.SUPABASE_KEY_3 }}"
